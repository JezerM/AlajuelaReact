import { StyleSheet } from "react-native";

export const colors = {
  primary: "#1F97F4",
};

export const sizes = {
  icon: {
    small: 24,
    medium: 28,
    big: 32,
  },
};

export const stylesheet = StyleSheet.create({
  primary: {
    color: colors.primary,
  },
});
